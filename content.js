const text = document.querySelectorAll('h1, h2, h3, h4, h5, p, li, td, caption, span, a')

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Professor Eleazar Fig')) 
{text[i].innerHTML = text[i].innerHTML.replace('Professor Eleazar Fig', 'HOGWARTS LEGACY SPOILER')}
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Fig Dies at the End')) 
{text[i].innerHTML = text[i].innerHTML.replace('Fig Dies at the End', 'HOGWARTS LEGACY SPOILER')}  
}


for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('PROFESSOR FIG DIES AT THE END')) 
{text[i].innerHTML = text[i].innerHTML.replace('PROFESSOR FIG DIES AT THE END', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('dies at the end of Hogwarts Legacy')) 
{text[i].innerHTML = text[i].innerHTML.replace('dies at the end of Hogwarts Legacy', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Professor Fig Dies at the End')) 
{text[i].innerHTML = text[i].innerHTML.replace('Professor Fig Dies at the End', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Professor Fig dies in every ending')) 
{text[i].innerHTML = text[i].innerHTML.replace('Professor Fig dies in every ending', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('the goblins were blamed for it')) 
{text[i].innerHTML = text[i].innerHTML.replace('the goblins were blamed for it', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Rookwood is the one that cursed Anne')) 
{text[i].innerHTML = text[i].innerHTML.replace('Rookwood is the one that cursed Anne', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Rookwood cursed')) 
{text[i].innerHTML = text[i].innerHTML.replace('Rookwood cursed', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Rookwood cursed Anne')) 
{text[i].innerHTML = text[i].innerHTML.replace('Rookwood cursed Anne', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('cursed Anne')) 
{text[i].innerHTML = text[i].innerHTML.replace('cursed Anne', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Rookwood framed')) 
{text[i].innerHTML = text[i].innerHTML.replace('Rookwood framed', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('and blood libel and quashing a slave rebellion are major plot points')) 
{text[i].innerHTML = text[i].innerHTML.replace('and blood libel and quashing a slave rebellion are major plot points', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Dark Wizards')) 
{text[i].innerHTML = text[i].innerHTML.replace('Dark Wizards', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Victor Rookwood')) 
{text[i].innerHTML = text[i].innerHTML.replace('Victor Rookwood', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('affected by player')) 
{text[i].innerHTML = text[i].innerHTML.replace('affected by player', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Fig dies')) 
{text[i].innerHTML = text[i].innerHTML.replace('Fig dies', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('fig dies')) 
{text[i].innerHTML = text[i].innerHTML.replace('fig dies', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Rookwoods the villain')) 
{text[i].innerHTML = text[i].innerHTML.replace('Rookwoods the villain', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('dies at the end of Hogwarts Legacy')) 
{text[i].innerHTML = text[i].innerHTML.replace('dies at the end of Hogwarts Legacy', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Your teacher dies at the end of the game')) 
{text[i].innerHTML = text[i].innerHTML.replace('Your teacher dies at the end of the game', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Rockwood is the one')) 
{text[i].innerHTML = text[i].innerHTML.replace('Rockwood is the one', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('fig dies')) 
{text[i].innerHTML = text[i].innerHTML.replace('fig dies', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('fig dies')) 
{text[i].innerHTML = text[i].innerHTML.replace('Fig Dies', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('fig died')) 
{text[i].innerHTML = text[i].innerHTML.replace('fig died', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Fig died')) 
{text[i].innerHTML = text[i].innerHTML.replace('Fig died', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Fig Died')) 
{text[i].innerHTML = text[i].innerHTML.replace('Fig Died', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('FIG DIES')) 
{text[i].innerHTML = text[i].innerHTML.replace('FIG DIES', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('FIG DIED')) 
{text[i].innerHTML = text[i].innerHTML.replace('FIG DIED', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('at the end of Hogwarts Legacy')) 
{text[i].innerHTML = text[i].innerHTML.replace('at the end of Hogwarts Legacy', 'HOGWARTS LEGACY SPOILER')}  
}


for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('at the end of hogwarts legacy')) 
{text[i].innerHTML = text[i].innerHTML.replace('at the end of hogwarts legacy', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('Rookwood')) 
{text[i].innerHTML = text[i].innerHTML.replace('Rookwood', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('rookwood')) 
{text[i].innerHTML = text[i].innerHTML.replace('rookwood', 'HOGWARTS LEGACY SPOILER')}  
}

for (let i=0; i < text.length; i++) {
if (text[i].innerHTML.includes('framed the goblins')) 
{text[i].innerHTML = text[i].innerHTML.replace('framed the goblins', 'HOGWARTS LEGACY SPOILER')}  
}